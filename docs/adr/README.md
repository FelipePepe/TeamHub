# ADR

This folder is reserved for Architecture Decision Records.

Current ADRs are consolidated in `docs/decisiones.md` while the project scaffolding is in progress.

Use `docs/adr/adr-template.md` for new ADRs once individual files are enabled.
