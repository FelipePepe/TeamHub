# Credenciales para tests E2E (Playwright)
# Copia este archivo a .env.e2e y rellena con tu usuario admin.
# .env.e2e no se sube a git (está en .gitignore).

E2E_USER=tu-admin@ejemplo.com
E2E_PASSWORD=tu-password-seguro

# E2E_MFA_SECRET: secret TOTP en base32 (código manual de MFA). OPCIONAL.
# Si NO lo tienes (normal si ya configuraste MFA y no lo guardaste):
#   Ejecuta los E2E en terminal: npm run test:e2e -- --workers=1
#   Cuando pida "Código MFA (6 dígitos de tu app):", escribe el código que muestra
#   tu app (Google Authenticator, etc.) y pulsa Enter. El test seguirá.
# Si SÍ lo tienes: ponlo aquí y los tests generarán el código automáticamente.
# Dónde se obtiene: solo se muestra UNA VEZ al configurar MFA ("O ingresa este código manualmente:").
E2E_MFA_SECRET=

# Opcional: si no usas las mismas que en frontend/.env
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
# NEXT_PUBLIC_API_HMAC_SECRET=el-mismo-valor-que-API_HMAC_SECRET-del-backend
